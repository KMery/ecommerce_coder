const msg_err_server = { message: 'Error en servidor' };
const msg_err_notFound = { message: 'No encontrado' };
const msg_err_notBody = { message: 'No existen atributos requeridos' }

module.exports = {
    msg_err_server,
    msg_err_notFound,
    msg_err_notBody
}